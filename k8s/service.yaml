apiVersion: v1
kind: Service
metadata:
  name: transporteparatodos
spec:
  type: NodePort

  selector:
    app: transporteparatodos

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32329